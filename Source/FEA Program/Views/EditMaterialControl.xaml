<UserControl x:Class="FEA_Program.Views.EditMaterialControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:FEA_Program.ViewModels"
             xmlns:conv="clr-namespace:FEA_Program.Converters"
             xmlns:mod="clr-namespace:FEA_Program.Models"
             xmlns:local="clr-namespace:FEA_Program.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="275">
    
    <UserControl.DataContext>
        <vm:MaterialEditVM/>
    </UserControl.DataContext>

    <Border Background="#F4F4F4" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header row -->
                <RowDefinition Height="Auto"/>
                <!-- Type row -->
                <RowDefinition Height="Auto"/>
                <!-- Name row -->
                <RowDefinition Height="Auto"/>
                <!-- Remaining fields -->
                <RowDefinition Height="*"/>
                <!-- Spacer -->
            </Grid.RowDefinitions>

            <!-- ===== Header: Title + Buttons ===== -->
            <DockPanel Grid.Row="0" Margin="0 0 0 20">
                <TextBlock
                       FontWeight="Bold"
                       FontSize="20"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="Add Material"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Editing}" Value="True">
                                    <Setter Property="Text" Value="Edit Material"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Right"
                        Margin="10 0 0 0">
                    <Button Content="Y" Width="30" Height="30" Margin="0 0 5 0" Command="{Binding AcceptCommand}"/>
                    <Button Content="N" Width="30" Height="30" Command="{Binding CancelCommand}"/>
                </StackPanel>
            </DockPanel>

            <!-- ===== Row 1: Type ===== -->
            <Grid Grid.Row="1" Margin="0 0 0 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Content="Type:"/>
                <ComboBox Grid.Column="1" HorizontalAlignment="Stretch" SelectedItem="{Binding Material.Model.Subtype}" ItemsSource="{Binding Source={conv:EnumBindingSource {x:Type mod:MaterialType}}}">
                </ComboBox>
            </Grid>

            <!-- ===== Row 2: Name ===== -->
            <Grid Grid.Row="2" Margin="0 0 0 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Content="Name:"/>
                <TextBox Grid.Column="1" HorizontalAlignment="Stretch" VerticalContentAlignment="Center" Text="{Binding Material.Model.Name}"/>
            </Grid>

            <!-- ===== Row 3+: Other Parameters ===== -->
            <StackPanel Grid.Row="3" Orientation="Vertical">
                <StackPanel>
                    <TextBlock Text="{Binding Source={StaticResource Units}, Path=Modulus.UnitString, StringFormat={} Youngs Modulus [{0}]:, Mode=OneWay}" Margin="0,5"/>
                    <TextBox Height="25" Margin="10, 0, 0, 0" Text="{Binding Material.E}"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="Poisson's Ratio [-]:"/>
                    <TextBox Height="25" Margin="10, 0, 0, 0" Text="{Binding Material.Model.V}"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="{Binding Source={StaticResource Units}, Path=Stress.UnitString, StringFormat={}Yield Strength [{0}]:, Mode=OneWay}" Margin="0,5"/>
                    <TextBox Height="25" Margin="10, 0, 0, 0" Text="{Binding Material.Sy}"/>
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="{Binding Source={StaticResource Units}, Path=Stress.UnitString, StringFormat={}Ultimate Strength [{0}]:, Mode=OneWay}" Margin="0,5"/>
                    <TextBox Height="25" Margin="10, 0, 0, 0" Text="{Binding Material.Sut}"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
