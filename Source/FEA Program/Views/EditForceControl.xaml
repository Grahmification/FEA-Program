<UserControl x:Class="FEA_Program.Views.EditForceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:FEA_Program.ViewModels"
             xmlns:conv="clr-namespace:FEA_Program.Converters"
             xmlns:mod="clr-namespace:FEA_Program.Models"
             xmlns:local="clr-namespace:FEA_Program.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="275">

    <UserControl.DataContext>
        <vm:ForceEditVM/>
    </UserControl.DataContext>

    <UserControl.Resources>
        <conv:NumberArrayToStringConverter x:Key="NumberArrayToStringConverter"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" Invert="True"/>
    </UserControl.Resources>

    <Border Background="#F4F4F4" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <!-- Header row -->
                <RowDefinition Height="Auto"/>
                <!-- Column Header row -->
                <RowDefinition Height="Auto"/>
                <!-- Value Rows -->
                <RowDefinition Height="Auto"/>
                <!-- Add Force Nodes List Selection -->
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ===== Header: Title + Buttons ===== -->
            <DockPanel Grid.Row="0" Margin="0 0 0 20">
                <TextBlock
                       FontWeight="Bold"
                       FontSize="20"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="Add Force"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Editing}" Value="True">
                                    <Setter Property="Text" Value="Edit Force"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Right"
                        Margin="10 0 0 0">
                    <Button Content="Y" Width="30" Height="30" Margin="0 0 5 0" Command="{Binding AcceptCommand}"/>
                    <Button Content="N" Width="30" Height="30" Command="{Binding CancelCommand}"/>
                </StackPanel>
            </DockPanel>

            <!-- ===== Row 1: Column Headers ===== -->
            <Grid Grid.Row="1" Margin="0 0 0 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0" Content="" Width="20"/>
                <TextBlock Grid.Column="1" Text="{Binding Source={StaticResource Units}, Path=Force.UnitString, StringFormat={}Force Component ({0}), Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            </Grid>

            <!-- ===== Row 2: Value Fields ===== -->
            <ItemsControl Grid.Row="2" ItemsSource="{Binding ForceComponents}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0 5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                        Text="{Binding Label}"
                                        Width="20"
                                        VerticalAlignment="Center"
                                        Margin="0,0,8,0"/>
                            <TextBox Grid.Column="1" 
                                        Text="{Binding Value}"
                                        Height="20"
                                        VerticalContentAlignment="Center"
                                        Margin="0 0 5 0"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- ===== Row 3: Add Force Nodes List Selection ===== -->
            <Grid Grid.Row="3" Margin="5 10 5 10" Visibility="{Binding Editing, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Node to Apply" Margin="5"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ListBox ItemsSource="{Binding Nodes}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="5,2">
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center"/>
                                    <TextBlock Margin="6,0,0,0" VerticalAlignment="Center">
                                        <Run Text="{Binding Node.Model.ID, Mode=OneWay}"/>
                                        <Run Text=" - ("/>
                                        <Run Text="{Binding Node.UserCoordinates, Converter={StaticResource NumberArrayToStringConverter}, Mode=OneWay}"/>
                                        <Run Text=")"/>
                                    </TextBlock>
                                </StackPanel>

                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>
            </Grid>

        </Grid>
    </Border>
</UserControl>
